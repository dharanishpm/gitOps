apiVersion: v1
kind: Service
metadata:
  name: gitops-app
  namespace: gitops-app
  labels:
    app: gitops-app
spec:
  type: LoadBalancer
  selector:
    app: gitops-app
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
